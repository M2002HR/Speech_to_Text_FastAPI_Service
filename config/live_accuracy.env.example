# Optional local .env overrides for accuracy-focused /live transcription.
# Copy these lines into your untracked .env after running setup.

PROVIDER_GROQ_MODEL=whisper-large-v3
LIVE_STT_MODEL=whisper-large-v3
LIVE_CHUNK_MS=6000
LIVE_AUDIO_TOPIC=
LIVE_LLM_CONTEXT_TOKENS=300
LIVE_STT_CONTEXT_TOKENS=160
